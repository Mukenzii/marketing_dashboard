# deps & build output (rebuilt inside the image)
node_modules
.next
out
build

# env & secrets — injected at runtime, never baked into the image
.env
.env.*
!.env.example
!.env.docker.example

# vcs & tooling
.git
.gitignore
*.md
.vscode
.idea
.DS_Store

# local/test artifacts
coverage
npm-debug.log*
*.tsbuildinfo
